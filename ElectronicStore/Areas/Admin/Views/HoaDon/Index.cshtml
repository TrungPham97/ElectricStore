@model IEnumerable<ElectronicStore.Areas.Admin.Models.HoaDon>

@{
    ViewBag.Title = "Index";
    Layout = "~/Areas/Admin/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.KhachHang.hoTen)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.ThanhToan.hoaDonID)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.loaiHoaDon)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.trangThai)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.KhachHang.hoTen)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.ThanhToan.hoaDonID)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.loaiHoaDon)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.trangThai)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.hoaDonID }) |
            @Html.ActionLink("Details", "Details", new { id=item.hoaDonID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.hoaDonID })
        </td>
    </tr>
}

</table>
